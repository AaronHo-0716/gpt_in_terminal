import os
import openai
from dotenv import load_dotenv
from rich.console import Console
from rich import pretty

console = Console()
pretty.install()
load_dotenv()

def changeModel():
    openai.api_key = os.environ["OPENAI_API_KEY"]
    models = openai.Model.list()
    console.print(models['data']['root'])
